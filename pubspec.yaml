name: tlob_jewellery_app
description: A luxury jewellery application.
publish_to: 'none'
version: 1.0.0+1

# 1. Add this block here:
environment:
  sdk: '>=3.0.0 <4.0.0'

# 2. Your existing dependencies block follows:
dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.0.2
  cupertino_icons: ^1.0.6
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  url_launcher: ^6.3.2
  google_sign_in: ^7.2.0
  provider: ^6.1.5+1
  video_player: ^2.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
